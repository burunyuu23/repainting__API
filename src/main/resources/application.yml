server:
  port: 5000
  servlet:
    context-path: /api

python:
  game-path: python/game.py


spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: {KEYCLOAK_URL}/realms/{REALM}/protocol/openid-connect/certs
#  datasource:
#    driver-class-name: org.postgresql.Driver
#    url: jdbc:postgresql://localhost:5432/danilkk_db
#    username: danilkk
#    password: 123
#  jpa:
#    database: postgresql
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#
springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api-docs

